<!DOCTYPE HTML>
<html>
<head>
    <title>QR CODE Generator</title>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="stylesheet" href="./css/style.css">

</head>
<body>
<div class="layout-grid">
    <section class="card-container">
        <div class=" card card-default">
            <h1 class="card-header text-center"> QR Generator</h1>
            <div class="card-body">
                <% if(QR_code){ %>
                    <h2>Scan this from your scanner</h2>
                    <div class="code-preview">
                        <img class="qr-code" src="<%= QR_code %>" width="350" height="350">
                        <nav class="cta-container">
                            <a class="button primary" href="/download?file_path=<%= img_src %>"> Download</a>
                            <a class="button outlined" href="/"> Generate another QR Code</a>
                        </nav>
                    </div>
                <% }else{ %>
                    <form class="generator-form" action="/" method="POST">
                        <div class="form-group">
                            <label for="url"> URL</label>
                            <input type="text" name="url" id="url" class="form-control" required
                                   placeholder="Enter URL here..">
                        </div>
                        <div class="form-group ">
                            <button class="button primary" type="submit" class="btn btn-primary">Generate</button>
                        </div>
                    </form>
                <% } %>
            </div>
        </div>
    </section>

</div>

</body>
</html>